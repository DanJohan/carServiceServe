<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <group android:checkableBehavior="single"
            android:id="@+id/nav_camera">
            <item
                android:id="@+id/user"
                android:icon="@drawable/user"
                android:title="Profile" /></group>
        <group android:checkableBehavior="single"
            android:id="@+id/nav_gallery">
            <item
                android:id="@+id/mycar"
                android:icon="@drawable/my_car"
                android:title="My car" /></group>
        <group android:checkableBehavior="single"
            android:id="@+id/nav_slideshow">
            <item
                android:id="@+id/history"
                android:icon="@drawable/history"
                android:title="History" /></group>
        <group android:checkableBehavior="single"
            android:id="@+id/nav_manage">
            <item
                android:id="@+id/about"
                android:icon="@drawable/about"
                android:title="About us" /></group>
        <group android:checkableBehavior="single"
            android:id="@+id/service">
            <item
                android:id="@+id/status"
                android:icon="@drawable/service_status"
                android:title="Service Status" /></group>
    </group>

</menu>